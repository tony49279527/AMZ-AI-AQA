# 复制本文件为 .env.local，只填下面 2 个 key，填一次就一直用（变量名不会改）
# 不要提交 .env.local 到 Git

# ---------- 只填这 2 个，一直用 ----------
OPENROUTER_API_KEY=        # 报告+问答用，去 https://openrouter.ai 拿
SCRAPINGBEE_API_KEY=       # 参考网页+YouTube用，去 https://www.scrapingbee.com 拿

# ---------- 下面不用动（有默认值） ----------
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=anthropic/claude-sonnet-4.5
NEXT_PUBLIC_APP_URL=http://localhost:3000
OPENROUTER_HTTP_REFERER=http://localhost:3000

# ---------- 可选 ----------
# API_ACCESS_TOKEN=          # 生产环境再填
# NEXT_PUBLIC_API_ACCESS_TOKEN=
# RAPIDAPI_KEY=              # 要拉亚马逊产品/评论时再填
# ACCESS_CODE=               # 上线后设一个访问码，仅知道的人能打开系统（只发给付费用户）
